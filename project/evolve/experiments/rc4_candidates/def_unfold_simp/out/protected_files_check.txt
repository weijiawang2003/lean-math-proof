RC4A def_unfold_simp — protected-file confirmation

git diff --stat HEAD over protected wrappers + ns24 (empty == untouched):
<<<empty == untouched>>>

Protected paths change status (empty == none):
<<<empty == none>>>

No RC4 release wrapper created (candidate only):
  (none — external additive evaluator used, as preferred)
